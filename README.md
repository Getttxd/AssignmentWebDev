# Assignment Website Application Development

## Overview

โปรเจกต์นี้คือระบบติดตามสถานะโดรน ที่พัฒนาขึ้นตามโจทย์ **Assignment #1 (API Server)** และ **Assignment #2 (Web Frontend)** โดยมีเป้าหมายในการบันทึกข้อมูล และแสดงผลข้อมูลของโดรนผ่าน API Gateway 

---

## Technologies Used

| องค์ประกอบ | เทคโนโลยีหลัก | บทบาท |
| :--- | :--- | :--- |
| **Backend (API Server)** | **Node.js, Express.js** | API Framework และ Gateway หลัก |
| | `dotenv`, `node-fetch`, `cors` | การจัดการ Environment Variables, HTTP Client, CORS Handling |
| **Frontend (Web Client)** | **Vite & react** | โครงสร้าง, ตรรกะฝั่ง Client (`app.jsx`) และการจัดการ State |
| | **Tailwind CSS & react icons** | การออกแบบ UI ที่รวดเร็วและ Icon |
| **Deployment server** | **Render** | Cloud Hosting สำหรับ API |
| **Deployment frontend** | **vercel** | Cloud Hosting สำหรับ Frontend|

---

# Assignment #1   (API Server)

API Server ทำหน้าที่เป็น **Gateway** เพื่อซ่อน URL และ API Token จริงจาก Frontend และจัดการการเรียก API ภายนอก

### Features

* **API Gateway:** ส่งต่อการเรียก API ไปยัง Drone Config Server และ Drone Log Server ภายนอก
* **Secure Logging:** ใช้ **Bearer Token** (`LOG_API_TOKEN` ใน `.env`) สำหรับการสร้าง Log Record ใหม่

### API Endpoints

| เส้นทาง (Route) | เมธอด HTTP | คำอธิบาย |
| :--- | :--- | :--- |
| **`/configs/:droneId`** | `GET` | ดึงข้อมูล Config (ID, Name, Light, Country, Condition) ของโดรนตาม ID ที่ระบุ  |
| **`/logs/:droneId`** | `GET` | ดึงรายการ Log ทั้งหมดของ `DRONE_ID` โดยแต่ละหน้าจะมีข้อมูลอยู่ 12 ชุด |
| **`/logs/:droneId/:page`** | `GET` | ดึงรายการ Log ทั้งหมดของ `DRONE_ID` โดยแต่ละหน้าจะมีข้อมูลอยู่ 12 ชุด (ใส่เลขหน้าได้)|
| **`/logs`** | `POST` | สร้าง Log Record ใหม่ (รับ `drone_id`, `drone_name`, `country`, `celsius`) |

### 🛠️ How to Run the Project
#### 1) Local Development

1.  **Install Dependencies:**
    ```bash
    npm install
    ```
2.  **Run Server:** (Server จะรันบน `http://localhost:3000` ตามค่าเริ่มต้น)
    ```bash
    npm run start
    ```
3.  **Access:** เปิดเบราว์เซอร์ไปที่ **`http://localhost:3000/`**

#### 2) Cloud Host Deployment (Render)

1. ** นำโปรเจคใส่ github **
2. ** ไปที https://render.com/ **
3. ** เลือก repository และ root node ที่จะ deploy **
4. ** deploy **

---

โปรเจกต์นี้ถูก Deploy เป็น **web service** บน Render เพื่อให้บริการ API
### API URL https://assignmentwebdev-server.onrender.com/
---

# Assignment #2   (Frontend)

 ประกอบด้วย 1 หน้า Home และ 3 หน้าหลักที่ทำงานร่วมกัน โดยใช้ react-router-dom ในการจัดการสถานะของแต่ละหน้า

### Pages & Functions

| หน้า (Page) | ไฟล์ .jsx | หน้าที่การทำงาน |
| :--- | :--- | :--- |
| **View Config** | `ConfigPage.jsx` |  ดึง Config จาก API และแสดงผลหน้า drone configuration   |
| **Temperature Form** | `TemperatureForm.jsx` | เป็น form ที่ให้ user กรอกค่าอุณหภูมิหน่วยเป็นองศาเซลเซียส (°C) และอ่านค่า จากฟอร์มที่ user กรอก, แล้วส่ง Log Record ใหม่ไปยัง `POST /logs` |
| **View Logs** | `LogsPage.jsx` | ดึง Log และสร้างตารางแสดงผลพร้อมระบบ **Pagination** (12 รายการ/หน้า) |

---

### 🛠️ How to Run the Project
#### 1) Local Development

1.  **Install Dependencies:**
    ```bash
    npm install
    ```
2.  **Run Server:** (Server จะรันบน `http://localhost:XXXX` ตามค่าเริ่มต้น)
    ```bash
    npm run start
    ```
3.  **Access:** เปิดเบราว์เซอร์ไปที่ **`http://localhost:XXXX/`**

#### 2) Cloud Host Deployment (Vercel)

1. ** นำโปรเจคใส่ github **
2. ** ไปที https://vercel.com/**
3. ** เลือก repository และ root node ที่จะ deploy **
4. ** deploy **

---

โปรเจกต์นี้ถูก Deploy เป็น vercel เพื่อให้บริการ Frontend


### Frontend URL https://drone-api-server-3r7c.onrender.com

  
